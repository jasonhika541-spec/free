# GitHub Actions Setup

Этот проект настроен для автоматической сборки .tipa файлов через GitHub Actions.

## 🔄 Автоматические сборки

### При каждом push:
- Запускается workflow `build.yml`
- Создается .tipa файл
- Артефакт доступен в разделе Actions

### При создании тега:
- Запускается workflow `release.yml`
- Создается GitHub Release
- .tipa файл прикрепляется к релизу

## 📋 Что происходит при сборке:

1. **Checkout** - скачивание кода
2. **Clean** - удаление проблемных директорий
3. **Setup Xcode** - настройка среды разработки
4. **Install Theos** - установка Theos framework
5. **Install dependencies** - установка ldid и других зависимостей
6. **Build** - компиляция проекта
7. **Create .tipa** - создание установочного файла
8. **Upload artifacts** - загрузка результатов

## 🚀 Как создать релиз:

```bash
# Создайте и отправьте тег
git tag v1.0.0
git push origin v1.0.0
```

Автоматически создастся релиз с файлами:
- `crepware-1.0.0.tipa` (версионный файл)
- `crepware.tipa` (общий файл)
- `checksums.txt` (контрольные суммы)

## 🔧 Настройка репозитория:

1. Убедитесь, что Actions включены в настройках репозитория
2. Проверьте права на создание релизов
3. При необходимости добавьте secrets в настройки

## 📁 Структура workflow:

```
.github/
├── workflows/
│   ├── build.yml      # Сборка при push
│   └── release.yml    # Релиз при тегах
└── README.md          # Этот файл
```

## 🐛 Решение проблем:

### Ошибка submodules:
- Удалена проблемная директория `READM1E.md`
- Отключен `submodules: recursive`

### Ошибка Theos:
- Добавлена проверка установки
- Подробное логирование процесса

### Ошибка прав доступа:
- Используется `sudo` для системных операций
- Правильная настройка владельца файлов

## 📊 Мониторинг сборок:

Проверяйте статус сборок в разделе [Actions](../actions) вашего репозитория.