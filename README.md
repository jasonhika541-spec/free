# so2_ex 0.36.1
so2 external hack iOS troll store

<img src="photo_2026-01-08_11-05-42.jpg" alt="UI preview" width="300" style="transform: rotate(90deg);">

The code is completely mine, I wrote it for some idiot

## 🚀 Автоматическая сборка

Проект настроен для автоматической сборки через GitHub Actions. При каждом push в main/master ветку создается .tipa файл.

### Доступные workflows:
- **build.yml** - Полная сборка с fallback для SDK
- **build-simple.yml** - Упрощенная сборка с Xcode SDK
- **release.yml** - Сборка релизов при создании тегов

### Скачать готовую сборку:
1. Перейдите в раздел [Actions](../../actions)
2. Выберите последний успешный build
3. Скачайте артефакт `crepware-tipa` или `crepware-tipa-simple`

### Создать релиз:
1. Создайте тег: `git tag v1.0.0`
2. Отправьте тег: `git push origin v1.0.0`
3. Автоматически создастся релиз с .tipa файлом

## 🔧 Локальная сборка

### Требования:
- macOS с Xcode
- [Theos](https://theos.dev/docs/installation)
- ldid: `brew install ldid`

### Сборка:
```bash
# Установите переменную окружения
export THEOS=/opt/theos

# Запустите скрипт сборки
chmod +x build.sh
./build.sh
```

Или вручную:
```bash
make clean
make package FINALPACKAGE=1 FOR_RELEASE=1
```

## 📱 Установка

1. Скачайте `crepware.tipa`
2. Установите через TrollStore или аналогичный инструмент
3. Убедитесь, что у вас есть необходимые права (jailbreak)

## ⚙️ Совместимость

- **iOS:** 14.0+
- **Устройства:** iPhone 12+ (arm64)
- **Тестировано:** iOS 16.6.1
- **Требует:** Jailbreak или TrollStore с правами отладки
- **Сборка:** Xcode 16.x, iOS SDK 16.5+

## 🛠️ Разработка

### Структура проекта:
- `sources/` - Основной код приложения
- `esp/` - ESP система и работа с памятью
- `headers/` - Приватные заголовки iOS
- `layout/` - Ресурсы и entitlements

### Ключевые компоненты:
- **Memory Access:** `esp/helpers/pid.mm` - чтение памяти процесса
- **Unity API:** `esp/unity_api/unity.mm` - работа с Unity объектами
- **ESP Rendering:** `esp/drawing_view/esp.mm` - отрисовка ESP
- **Touch Simulation:** `sources/KIF/` - симуляция касаний

## 🔒 Безопасность

Проект использует множество системных entitlements для доступа к:
- Памяти других процессов (`task_for_pid-allow`)
- Системным событиям (`com.apple.system-task-ports`)
- HID событиям (`com.apple.private.hid.client.*`)

## ⚠️ Дисклеймер

Этот проект предназначен только для образовательных целей. Использование в онлайн играх может нарушать правила сервиса.
